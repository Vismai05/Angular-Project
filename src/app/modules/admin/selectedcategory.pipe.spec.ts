import { SelectedcategoryPipe } from './selectedcategory.pipe';

describe('SelectedcategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedcategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
